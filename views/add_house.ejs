<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Procurement System - Login/Register</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f1f5f9;
    }

    .auth-container {
      max-width: 400px;
      margin: auto;
      margin-top: 60px;
      padding: 30px;
      background: white;
      border-radius: 15px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    }

    .toggle-btn {
      cursor: pointer;
      text-decoration: underline;
      color: #0d6efd;
    }

    .form-title {
      font-size: 24px;
      font-weight: 600;
      margin-bottom: 20px;
      text-align: center;
    }
  </style>
</head>
<body>

  <div class="auth-container">
    <!-- Registration Form -->
    <div id="register-form">
      <div class="form-title">Add Property</div>
      <form>
        <div class="mb-3">
          <label for="name" class="form-label">House Type</label>
          <input type="text" class="form-control" id="name" required />
        </div>
        <div class="mb-3">
            <label for="name" class="form-label">Price</label>
            <input type="number" class="form-control" id="name" required />
          </div>
        <div class="mb-3">
          <label for="role" class="form-label">Nature</label>
          <select class="form-select" id="role" required>
            <option value="">Select nature</option>
            <option>sell</option>
            <option>rent</option>
           
          </select>
        </div>
      
        <div class="mb-3">
          <label for="regPassword" class="form-label">Location</label>
          <input type="password" class="form-control" id="regPassword" required />
        </div>
        <button type="submit" class="btn btn-primary w-100">Submit</button>
      </form>
      <div class="text-center mt-3">
        Additional details <span class="toggle-btn" onclick="toggleForms()">Next >></span>
      </div>
    </div>

    <!-- Login Form -->
    <div id="login-form" style="display: none;">
      <div class="form-title">Additional details</div>
      <form>
        <div class="mb-3">
          <label for="regEmail" class="form-label">Size</label>
          <input type="text" class="form-control" id="regEmail" required />
        </div>
        <div class="mb-3">
          <label for="loginEmail" class="form-label">No of beds</label>
          <input type="text" class="form-control" id="loginEmail" required />
        </div>
        <div class="mb-3">
          <label for="loginPassword" class="form-label">No of Bath</label>
          <input type="text" class="form-control" id="loginPassword" required />
        </div>
        <button type="submit" class="btn btn-success w-100">Finish</button>
      </form>
      <div class="text-center mt-3">
        Go back <span class="toggle-btn" onclick="toggleForms()"><<</span>
      </div>
    </div>
  </div>

  <script>
    function toggleForms() {
      const regForm = document.getElementById("register-form");
      const loginForm = document.getElementById("login-form");
      if (regForm.style.display === "none") {
        regForm.style.display = "block";
        loginForm.style.display = "none";
      } else {
        regForm.style.display = "none";
        loginForm.style.display = "block";
      }
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

